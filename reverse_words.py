"""
Reverse the order of words in a given sentence(an array of characters).
"Hello World" -> "World Hello"
"""


def reverse_words(sentence: list[str]) -> str:    # sentence here is an array of characters
    #TODO: Write - Your - Code
    center: int = len(sentence) // 2 if len(sentence) % 2 == 1 else None

    reversed_sentence: list[str] = []

    if center is None:
        reverse_index: int = -1
        for i in range(0, len(sentence)):

            pass

    else:
        for i in range(0, len(sentence)):
            pass

        pass


    return sentence
